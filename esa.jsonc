{
  // EdgeOne 静态网站部署配置
  // Reading Garden v3 - 纯静态网站配置

  // 静态资源目录（相对于项目根目录）
  // 使用 "." 表示整个项目根目录都是静态资源
  "assets": ".",

  // 入口文件（可选）
  "index": "index.html",

  // 404页面（可选）
  "error": "index.html",

  // 构建输出目录（对于纯静态网站，等同于根目录）
  "output": "."
}
